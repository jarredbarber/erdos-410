{
  "workflow": "math-research",
  "notify_cmd": "curl -s -H 'Title: {{title}}' -H 'Priority: {{priority}}' -d '{{message}}' https://ntfy.sh/gigahector-vps",
  "overseer": {
    "enabled": true
  },
  "defaults": {
    "priority": 2,
    "size": "medium",
    "type": "task"
  }
}
